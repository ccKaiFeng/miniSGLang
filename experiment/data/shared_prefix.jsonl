{"id":"shared_prefix_001","prompt":"你是一名严谨的系统性能分析助手。请始终先给出结论，再说明依据。背景资料：miniSGLang 是一个用于大语言模型推理服务的实验性系统，包含 tokenizer、scheduler、engine、paged KV cache、attention backend 和 OpenAI 兼容 API。ZipCache v1 在 attention kernel 前后插入 KV cache 压缩和解压逻辑，不修改 CUDA kernel，也不修改 attention 数学公式。它通过 probe token 估计 token 重要性，将重要 token 用较高 bit 量化，将不重要 token 用较低 bit 量化。当前 v1 的限制是仍然保留原始 GPU KV pool，所以 nvidia-smi 看到的 reserved memory 不一定下降。请基于这些背景，解释为什么 ZipCache v1 的压缩率提高不一定直接等于 GPU 显存下降。","max_tokens":128}
{"id":"shared_prefix_002","prompt":"你是一名严谨的系统性能分析助手。请始终先给出结论，再说明依据。背景资料：miniSGLang 是一个用于大语言模型推理服务的实验性系统，包含 tokenizer、scheduler、engine、paged KV cache、attention backend 和 OpenAI 兼容 API。ZipCache v1 在 attention kernel 前后插入 KV cache 压缩和解压逻辑，不修改 CUDA kernel，也不修改 attention 数学公式。它通过 probe token 估计 token 重要性，将重要 token 用较高 bit 量化，将不重要 token 用较低 bit 量化。当前 v1 的限制是仍然保留原始 GPU KV pool，所以 nvidia-smi 看到的 reserved memory 不一定下降。请基于这些背景，列出 main 分支和 ZipCache 分支压测时应该观察的核心指标。","max_tokens":128}
{"id":"shared_prefix_003","prompt":"你是一名严谨的系统性能分析助手。请始终先给出结论，再说明依据。背景资料：miniSGLang 是一个用于大语言模型推理服务的实验性系统，包含 tokenizer、scheduler、engine、paged KV cache、attention backend 和 OpenAI 兼容 API。ZipCache v1 在 attention kernel 前后插入 KV cache 压缩和解压逻辑，不修改 CUDA kernel，也不修改 attention 数学公式。它通过 probe token 估计 token 重要性，将重要 token 用较高 bit 量化，将不重要 token 用较低 bit 量化。当前 v1 的限制是仍然保留原始 GPU KV pool，所以 nvidia-smi 看到的 reserved memory 不一定下降。请基于这些背景，说明如果 ZipCache 分支 TTFT 增加，可能有哪些原因。","max_tokens":128}
{"id":"shared_prefix_004","prompt":"你是一名严谨的系统性能分析助手。请始终先给出结论，再说明依据。背景资料：miniSGLang 是一个用于大语言模型推理服务的实验性系统，包含 tokenizer、scheduler、engine、paged KV cache、attention backend 和 OpenAI 兼容 API。ZipCache v1 在 attention kernel 前后插入 KV cache 压缩和解压逻辑，不修改 CUDA kernel，也不修改 attention 数学公式。它通过 probe token 估计 token 重要性，将重要 token 用较高 bit 量化，将不重要 token 用较低 bit 量化。当前 v1 的限制是仍然保留原始 GPU KV pool，所以 nvidia-smi 看到的 reserved memory 不一定下降。请基于这些背景，设计一个验证输出正确率的简单实验。","max_tokens":128}
{"id":"shared_prefix_005","prompt":"你是一名严谨的系统性能分析助手。请始终先给出结论，再说明依据。背景资料：miniSGLang 是一个用于大语言模型推理服务的实验性系统，包含 tokenizer、scheduler、engine、paged KV cache、attention backend 和 OpenAI 兼容 API。ZipCache v1 在 attention kernel 前后插入 KV cache 压缩和解压逻辑，不修改 CUDA kernel，也不修改 attention 数学公式。它通过 probe token 估计 token 重要性，将重要 token 用较高 bit 量化，将不重要 token 用较低 bit 量化。当前 v1 的限制是仍然保留原始 GPU KV pool，所以 nvidia-smi 看到的 reserved memory 不一定下降。请基于这些背景，解释 probe token 为什么会带来额外开销。","max_tokens":128}
